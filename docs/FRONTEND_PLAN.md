# VetFlow — план фронтенд-разработки

Дата аудита: 22.07.2026. Объём кода: ~1240 строк, 29 файлов.

---

## 1. Аудит текущего состояния

### Что сделано хорошо

- Единый источник типов (`src/types/index.ts`) — контракт для MSW и будущего бэкенда.
- Тонкий `api/client.ts` + хуки TanStack Query — замена мока на реальный API затронет только слой fetch.
- MSW с in-memory мутациями — фронт разрабатывается независимо от бэкенда.
- Дизайн-токены в `theme.ts`, узнаваемый визуальный мотив.
- Строгий TypeScript, маленькие компоненты, понятная структура папок.

### Найденные проблемы (техдолг)

| # | Проблема | Где | Серьёзность |
|---|---|---|---|
| 1 | `import.meta.env.PUBLIC_API_URL` — Vite отдаёт только переменные с префиксом `VITE_`. Сейчас всегда undefined → fallback `/api`. **Переключение на реальный бэкенд через env не сработает.** | `api/client.ts:5` | 🔴 Блокер для этапа интеграции |
| 2 | Хардкод hex-цветов вопреки собственному правилу: `STATUS_COLOR` в SchedulePage, `#2F6F52`/`#B94A3E` в PatientDetailPage, пропсы `Stamp $color="#2F6F52"` | SchedulePage, PatientDetailPage | 🟠 |
| 3 | `STATUS_COLOR`/`STATUS_LABEL` живут внутри страницы — понадобятся и в booking, и в карточке | SchedulePage | 🟠 |
| 4 | Нет состояний ошибки и пустых данных — только «Загрузка...». Упавший запрос = вечная загрузка | все страницы | 🟠 |
| 5 | Расписание игнорирует `date` приёма (показывает все), `durationSlots > 1` не блокирует нижние слоты, нет навигации по дням | SchedulePage | 🟠 |
| 6 | Статус «Активен» захардкожен, не читает `animal.status` | PatientDetailPage:188 | 🟡 |
| 7 | Инлайн-стиль `style={{ display: 'flex' ... }}` вопреки правилу «только styled-components» | PatientDetailPage:212 | 🟡 |
| 8 | «{ageYears} года» — нет склонения (1 год / 2 года / 5 лет) | PatientDetailPage:182 | 🟡 |
| 9 | `vitals.weightKg && ...` — вес 0 или температура 0 скроют строку; использовать `!= null` | PatientDetailPage | 🟡 |
| 10 | `doctorName: 'Др. Иванов'` захардкожен при создании визита; надо брать текущего пользователя | mocks/handlers.ts:72 | 🟡 |
| 11 | Модалки без focus-trap, Escape, aria-атрибутов | NewVisitModal, NewPatientModal | 🟡 |
| 12 | Нет обработки 401 (протухший токен) — при реальном API пользователь застрянет | api/client.ts | 🟠 |
| 13 | `@types/styled-components@5` конфликтует с styled-components v6 (у v6 свои типы) — удалить | package.json | 🟡 |
| 14 | oxlint настроен, но нет скрипта `lint` в package.json; нет тестов вообще | package.json | 🟡 |
| 15 | Vite 4 устарел (актуален 6+), `engines: node >=16` устарел | package.json | 🟡 |
| 16 | Нет адаптива: сайдбар фиксированный 220px, сетки ломаются < ~1000px | layout, страницы | 🟡 |

---

## 2. Дорожная карта

Приоритет модулей по ценности для клиники: **Онлайн-запись → Напоминания → Склад → Аналитика** (запись и напоминания приносят клиентов, склад и аналитика — внутренняя оптимизация).

### Этап 0 — Фундамент ✅ выполнен 22.07.2026

Закрыть техдолг до наращивания фич, пока кодовая база маленькая.

- [x] `client.ts`: `PUBLIC_API_URL` → `VITE_API_URL`, добавить `.env.example`
- [x] Вынести статусы в `src/constants/appointment.ts` (label + tone), новые токены темы: `ash`, `mist`, `clayDark`, `inkMuted`, `inkBorder`
- [x] Убрать все хардкод-hex из страниц; `Stamp` переведён на `$tone`, `IconCircle`/`AnimalAvatar` берут цвет из темы
- [x] `LoadingState` / `ErrorState` / `EmptyState` в `components/ui/QueryState.tsx`, подключены на всех страницах
- [x] Утилита `plural()` в `src/utils/plural.ts` (+ unit-тест)
- [x] Обработка 401 в `client.ts`: очистка токена + редирект на `/login`
- [x] Исправлены пункты 6, 7, 8, 9, 10 аудита; мок-логин выдаёт токен с id пользователя, визит записывается от имени вошедшего врача
- [x] `package.json`: скрипты `lint`, `typecheck`, `test`, `test:watch`; удалён `@types/styled-components`
- [x] Vitest + RTL + MSW (node): 7 тестов — логин (успех/ошибка), список ЭМК (загрузка/поиск/пусто), карточка (данные/добавление визита), plural
- [x] Бонус из этапа 7: Vite 4 → 5, `@vitejs/plugin-react` 3 → 4, engines node >=18 (потребовалось для Vitest 2)

Проверено: `tsc -b` ✅, `oxlint src` (0 ошибок) ✅, `vitest run` (7/7) ✅, `vite build` ✅.
После `git pull` выполнить `npm install` — добавлены dev-зависимости.

### Этап 1 — Достройка существующих страниц (~4–5 дней)

**Расписание** (`/schedule`)
- [ ] Навигация по датам: сегодня / ± день, фильтрация приёмов по `date`
- [ ] Корректный рендер `durationSlots > 1` (скрывать перекрытые слоты)
- [ ] Клик по приёму → поповер/модалка с деталями и ссылкой на карточку животного
- [ ] Клик по пустому слоту → модалка создания приёма (`POST /api/appointments` + handler в MSW)
- [ ] Смена статуса приёма (подтверждён → завершён)

**Карточка животного** (`/patients/:id`)
- [ ] Рабочие вкладки: Диагнозы (сгруппированная история), Назначения (все prescriptions), Документы (заглушка со списком файлов)
- [ ] Редактирование аллергий и витальных показателей (`PATCH /api/animals/:id`)
- [ ] Статус active/inactive с переключением

**Список ЭМК** (`/patients`)
- [ ] Пагинация или виртуализация (задел на рост базы)
- [ ] Фильтры: вид животного, статус

### Этап 2 — Онлайн-запись `/booking` (~4–5 дней)

Самый ценный модуль: публичная страница записи + админ-вью заявок.

- [ ] Типы: `BookingRequest { id, animalName, ownerName, phone, preferredDate, preferredTime, doctorId?, reason, status: 'new'|'approved'|'rejected' }`
- [ ] MSW: `GET/POST /api/bookings`, `PATCH /api/bookings/:id`
- [ ] Публичный роут `/book` (без ProtectedRoute): форма записи с выбором врача и свободных слотов (переиспользовать данные `/api/appointments`)
- [ ] Админ-страница `/booking`: список заявок со штампами статусов, подтверждение → создаёт `Appointment`
- [ ] Бейдж количества новых заявок в сайдбаре

### Этап 3 — Напоминания `/reminders` (~3 дня)

- [ ] Типы: `Reminder { id, animalId, type: 'vaccination'|'checkup'|'medication', dueDate, status, note }`
- [ ] MSW-handlers + данные (просроченные / ближайшие / будущие)
- [ ] Страница: группировка по срочности (просрочено = clay, скоро = kraft), отметка «выполнено»
- [ ] Блок ближайших напоминаний в карточке животного

### Этап 4 — Склад `/stock` (~3–4 дня)

- [ ] Типы: `StockItem { id, name, category, quantity, unit, minQuantity, expiryDate? }`
- [ ] Страница: таблица с поиском, штампы «заканчивается» / «просрочен»
- [ ] Приход/списание (модалка), связка со списанием препаратов при визите — отложить до реального API

### Этап 5 — Аналитика `/analytics` (~3 дня)

- [ ] Подключить лёгкую chart-библиотеку (recharts)
- [ ] Метрики: приёмы по дням/врачам, загрузка врачей, новые пациенты, топ диагнозов
- [ ] MSW: `GET /api/analytics/summary` (агрегация на «бэке», не на клиенте — так контракт переживёт реальный API)

### Этап 6 — Интеграция с реальным API (по готовности бэкенда)

- [ ] `VITE_API_URL` на staging-бэкенд, MSW включать только в dev (`import.meta.env.DEV`)
- [ ] Реальный JWT: refresh-token или редирект по истечении, интерцептор уже готов с этапа 0
- [ ] Сверка контракта: типы из `types/index.ts` — эталон, расхождения чинить на бэкенде
- [ ] Ретраи/staleTime в QueryClient под реальную сеть, optimistic updates для мутаций статусов

### Этап 7 — Качество и полировка (параллельно/в конце)

- [ ] Адаптив: сайдбар → выдвижной на <1024px, сетка расписания → горизонтальный скролл, карточка → одна колонка
- [ ] A11y: focus-trap и Escape в модалках, aria-label на иконках, контраст
- [ ] Code splitting: `React.lazy` по страницам
- [x] Обновление Vite 4 → 5, node engines (сделано в этапе 0; при желании позже 5 → 6+)
- [ ] CI (GitHub Actions): `lint` + `typecheck` + `test` на PR
- [ ] Подготовка к i18n (uz/ru): строки пока хардкод — вынести в словарь только при реальной потребности, не раньше

---

## 3. Конвенции для новых модулей (порядок работы над любой фичей)

1. Типы → `src/types/index.ts`
2. Мок-данные → `mocks/data.ts`, handler → `mocks/handlers.ts`
3. Fetch-функции + хуки → `src/api/<module>.ts`
4. UI: переиспользовать `Card`, `Stamp`, `Button`, `Input`, `QueryState`
5. Роут в `App.tsx` + пункт сайдбара (`enabled: true`)
6. Smoke-тест на happy path

## 4. Definition of Done для каждой задачи

- `tsc -b` и `oxlint` без ошибок
- Нет hex-цветов вне `theme.ts`, нет инлайн-стилей, нет локальных дублей типов
- Состояния загрузки/ошибки/пусто обработаны
- Интерфейс на русском
